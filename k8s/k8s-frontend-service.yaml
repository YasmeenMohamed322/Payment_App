apiVersion: v1
kind: Service
metadata:
  name: simplepay-frontend
  namespace: simplepay
spec:
  type: NodePort
  selector:
    app: simplepay-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080

